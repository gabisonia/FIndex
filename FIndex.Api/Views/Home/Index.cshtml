@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FIndex - Face Recognition Search</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-slate-50 via-white to-blue-50 min-h-screen flex flex-col">
    <!-- Navigation Bar -->
    <nav class="bg-white/80 backdrop-blur-md border-b border-gray-100">
        <div class="container mx-auto px-4 py-4">
            <div class="flex items-center justify-between">
                <div class="flex items-center">
                    <div class="w-10 h-10 bg-gradient-to-br from-blue-400 to-purple-600 rounded-xl flex items-center justify-center mr-3">
                        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                        </svg>
                    </div>
                    <h1 class="text-2xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">FIndex</h1>
                </div>
                <div class="flex items-center space-x-6">
                    <a href="#" class="text-gray-600 hover:text-gray-900 transition">How it works</a>
                    <a href="#" class="text-gray-600 hover:text-gray-900 transition">About</a>
                    <button class="bg-gradient-to-r from-blue-500 to-purple-600 text-white px-4 py-2 rounded-full text-sm font-medium hover:shadow-lg transition">
                        Sign In
                    </button>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="flex-grow container mx-auto px-4 py-12">
        <div class="max-w-5xl mx-auto">
            <!-- Headline -->
            <div class="text-center mb-12">
                <h2 class="text-5xl font-bold bg-gradient-to-r from-gray-800 to-gray-600 bg-clip-text text-transparent mb-4">
                    Discover Faces with AI
                </h2>
                <p class="text-gray-600 text-lg">Upload an image and let our AI identify faces instantly</p>
            </div>

            <!-- Step Indicators -->
            <div class="flex items-center justify-center mb-12">
                <div class="flex items-center space-x-4">
                    <!-- Step 1 -->
                    <div class="flex items-center">
                        <div id="step1Indicator" class="w-10 h-10 bg-gradient-to-r from-blue-500 to-purple-600 text-white rounded-full flex items-center justify-center font-bold">
                            1
                        </div>
                        <span class="ml-2 text-gray-700 font-medium">Upload Image</span>
                    </div>
                    <!-- Connector -->
                    <div class="w-20 h-1 bg-gray-300 relative">
                        <div id="step1to2Progress" class="absolute top-0 left-0 h-full bg-gradient-to-r from-blue-500 to-purple-600 transition-all duration-500" style="width: 0%"></div>
                    </div>
                    <!-- Step 2 -->
                    <div class="flex items-center">
                        <div id="step2Indicator" class="w-10 h-10 bg-gray-300 text-gray-600 rounded-full flex items-center justify-center font-bold">
                            2
                        </div>
                        <span class="ml-2 text-gray-500 font-medium">Detect Faces</span>
                    </div>
                    <!-- Connector -->
                    <div class="w-20 h-1 bg-gray-300 relative">
                        <div id="step2to3Progress" class="absolute top-0 left-0 h-full bg-gradient-to-r from-blue-500 to-purple-600 transition-all duration-500" style="width: 0%"></div>
                    </div>
                    <!-- Step 3 -->
                    <div class="flex items-center">
                        <div id="step3Indicator" class="w-10 h-10 bg-gray-300 text-gray-600 rounded-full flex items-center justify-center font-bold">
                            3
                        </div>
                        <span class="ml-2 text-gray-500 font-medium">View Results</span>
                    </div>
                </div>
            </div>

            <!-- Step 1: Upload Section -->
            <div id="step1Content" class="bg-white/70 backdrop-blur-sm rounded-3xl shadow-xl border border-gray-100 p-8 mb-8">
                <div class="flex flex-col items-center">
                    <div class="w-32 h-32 bg-gradient-to-br from-blue-100 to-purple-100 rounded-3xl flex items-center justify-center mb-6">
                        <svg class="w-16 h-16 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"></path>
                        </svg>
                    </div>
                    <input type="file" id="imageUpload" accept="image/jpeg,image/jpg,image/png,image/gif,image/webp" style="display: none;">
                    <button type="button" id="uploadButton" class="bg-gradient-to-r from-blue-500 to-purple-600 text-white font-semibold py-4 px-8 rounded-2xl transition duration-300 ease-in-out transform hover:scale-105 hover:shadow-xl flex items-center space-x-2">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                        </svg>
                        <span>Choose Image</span>
                    </button>
                    <p class="text-gray-500 mt-3 text-sm">Drag & drop or click to upload • JPG, PNG, GIF</p>
                </div>

            </div>

            <!-- Step 2: Preview and Detect -->
            <div id="step2Content" class="hidden">
                <div class="bg-white/70 backdrop-blur-sm rounded-3xl shadow-xl border border-gray-100 p-8 mb-8">
                    <div class="bg-gradient-to-r from-blue-50 to-purple-50 rounded-2xl p-6">
                        <h3 class="text-lg font-semibold text-gray-700 mb-4 flex items-center">
                            <svg class="w-5 h-5 mr-2 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                            </svg>
                            Image Ready for Analysis
                        </h3>
                        <div class="flex justify-center mb-6">
                            <img id="imagePreview" class="max-w-full max-h-96 rounded-xl shadow-xl" alt="Preview">
                        </div>
                        <div class="flex gap-4">
                            <button id="detectButton" class="flex-1 bg-gradient-to-r from-green-500 to-emerald-600 text-white font-semibold py-4 px-6 rounded-2xl transition duration-300 ease-in-out transform hover:scale-105 hover:shadow-xl flex items-center justify-center space-x-2">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                                </svg>
                                <span>Detect Faces</span>
                            </button>
                            <button id="changeImageButton" class="bg-gray-500 text-white font-semibold py-4 px-6 rounded-2xl transition duration-300 ease-in-out transform hover:scale-105 hover:shadow-xl flex items-center justify-center space-x-2">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
                                </svg>
                                <span>Change Image</span>
                            </button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Step 3: Results Section -->
            <div id="step3Content" class="hidden">
                <div class="bg-white/70 backdrop-blur-sm rounded-3xl shadow-xl border border-gray-100 p-8 mb-8">
                    <div class="flex justify-between items-center mb-8">
                        <div>
                            <h3 class="text-3xl font-bold bg-gradient-to-r from-gray-800 to-gray-600 bg-clip-text text-transparent">Matches Found</h3>
                            <p class="text-gray-600 mt-2">We found 3 potential matches in our database</p>
                        </div>
                        <button id="restartButton" class="bg-gradient-to-r from-gray-500 to-gray-600 text-white font-semibold py-3 px-6 rounded-2xl transition duration-300 ease-in-out transform hover:scale-105 hover:shadow-xl flex items-center space-x-2">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
                            </svg>
                            <span>Start Over</span>
                        </button>
                    </div>
                    
                    <div class="grid md:grid-cols-3 gap-6">
                    <!-- Result Card 1 -->
                    <div class="bg-white/70 backdrop-blur-sm rounded-3xl shadow-xl border border-gray-100 p-6 transform transition duration-500 ease-in-out opacity-0 translate-y-4 hover:scale-105" id="result1">
                        <div class="text-center">
                            <div class="relative inline-block mb-4">
                                <img src="https://dxbhsrqyrr690.cloudfront.net/sidearm.nextgen.sites/campbellsville.sidearmsports.com/images/2018/7/31/Alex_Adams_2018_web.jpg?width=300" alt="Match" class="w-32 h-32 rounded-2xl object-cover mx-auto">
                                <div class="absolute -bottom-2 -right-2 bg-gradient-to-r from-green-400 to-emerald-500 text-white text-xs font-bold px-3 py-1 rounded-full shadow-lg">
                                    95%
                                </div>
                            </div>
                            <h4 class="text-xl font-bold text-gray-800 mb-4">Alex Adams</h4>
                            <div class="space-y-2 text-sm text-left bg-gray-50 rounded-xl p-4 mb-4">
                                <p class="flex items-center text-gray-600">
                                    <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                    </svg>
                                    Los Angeles, CA
                                </p>
                                <p class="flex items-center text-gray-600">
                                    <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                                    </svg>
                                    Verified Profile
                                </p>
                            </div>
                            <a href="https://www.youtube.com/watch?v=qwSOe_A3CNQ" class="block w-full bg-gradient-to-r from-blue-500 to-purple-600 text-white text-sm font-medium py-3 px-4 rounded-xl hover:shadow-lg transition">
                                View Profile
                            </a>
                        </div>
                    </div>

                    <!-- Result Card 2 -->
                    <div class="bg-white/70 backdrop-blur-sm rounded-3xl shadow-xl border border-gray-100 p-6 transform transition duration-500 ease-in-out opacity-0 translate-y-4 hover:scale-105" id="result2">
                        <div class="text-center">
                            <div class="relative inline-block mb-4">
                                <img src="https://ei.phncdn.com/(m=ewILGCjadOf)(mh=joWo_BmQ2R5P3Mx4)e799c60b-510d-475a-a5c7-c1da73eb9acc.jpg" alt="Match" class="w-32 h-32 rounded-2xl object-cover mx-auto">
                                <div class="absolute -bottom-2 -right-2 bg-gradient-to-r from-yellow-400 to-orange-500 text-white text-xs font-bold px-3 py-1 rounded-full shadow-lg">
                                    82%
                                </div>
                            </div>
                            <h4 class="text-xl font-bold text-gray-800 mb-4">Blake Blossom</h4>
                            <div class="space-y-2 text-sm text-left bg-gray-50 rounded-xl p-4 mb-4">
                                <p class="flex items-center text-gray-600">
                                    <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                    </svg>
                                    Phoenix, AZ
                                </p>
                                <p class="flex items-center text-gray-600">
                                    <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                                    </svg>
                                    Verified Profile
                                </p>
                            </div>
                            <a href="https://www.youtube.com/watch?v=qwSOe_A3CNQ" class="block w-full bg-gradient-to-r from-blue-500 to-purple-600 text-white text-sm font-medium py-3 px-4 rounded-xl hover:shadow-lg transition">
                                View Profile
                            </a>
                        </div>
                    </div>

                    <!-- Result Card 3 -->
                    <div class="bg-white/70 backdrop-blur-sm rounded-3xl shadow-xl border border-gray-100 p-6 transform transition duration-500 ease-in-out opacity-0 translate-y-4 hover:scale-105" id="result3">
                        <div class="text-center">
                            <div class="relative inline-block mb-4">
                                <img src="https://ei.phncdn.com/(m=bLWsSeKlbyaT)(mh=4cBs_gAdRRiUGawj)53db4808-ad98-44bb-98d4-f81cc92ec115.jpg" alt="Match" class="w-32 h-32 rounded-2xl object-cover mx-auto">
                                <div class="absolute -bottom-2 -right-2 bg-gradient-to-r from-purple-400 to-pink-500 text-white text-xs font-bold px-3 py-1 rounded-full shadow-lg">
                                    76%
                                </div>
                            </div>
                            <h4 class="text-xl font-bold text-gray-800 mb-4">Scott Stark</h4>
                            <div class="space-y-2 text-sm text-left bg-gray-50 rounded-xl p-4 mb-4">
                                <p class="flex items-center text-gray-600">
                                    <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                    </svg>
                                    Miami, FL
                                </p>
                                <p class="flex items-center text-gray-600">
                                    <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                                    </svg>
                                    Verified Profile
                                </p>
                            </div>
                            <a href="https://www.youtube.com/watch?v=qwSOe_A3CNQ" class="block w-full bg-gradient-to-r from-blue-500 to-purple-600 text-white text-sm font-medium py-3 px-4 rounded-xl hover:shadow-lg transition">
                                View Profile
                            </a>
                        </div>
                    </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-white/80 backdrop-blur-md border-t border-gray-100 py-8 mt-12">
        <div class="container mx-auto px-4">
            <div class="flex flex-col md:flex-row items-center justify-between">
                <div class="flex items-center mb-4 md:mb-0">
                    <div class="w-8 h-8 bg-gradient-to-br from-blue-400 to-purple-600 rounded-lg flex items-center justify-center mr-2">
                        <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                        </svg>
                    </div>
                    <p class="text-gray-600">&copy; 2025 FIndex. All rights reserved.</p>
                </div>
                <div class="flex space-x-6">
                    <a href="#" class="text-gray-600 hover:text-gray-900 transition">Privacy</a>
                    <a href="#" class="text-gray-600 hover:text-gray-900 transition">Terms</a>
                    <a href="#" class="text-gray-600 hover:text-gray-900 transition">Contact</a>
                </div>
            </div>
        </div>
    </footer>

    <script>
        // Handle image upload and preview
        const imageUpload = document.getElementById('imageUpload');
        const uploadButton = document.getElementById('uploadButton');
        const imagePreview = document.getElementById('imagePreview');
        const detectButton = document.getElementById('detectButton');
        const changeImageButton = document.getElementById('changeImageButton');
        const restartButton = document.getElementById('restartButton');
        
        // Step content containers
        const step1Content = document.getElementById('step1Content');
        const step2Content = document.getElementById('step2Content');
        const step3Content = document.getElementById('step3Content');
        
        // Step indicators
        const step1Indicator = document.getElementById('step1Indicator');
        const step2Indicator = document.getElementById('step2Indicator');
        const step3Indicator = document.getElementById('step3Indicator');
        const step1to2Progress = document.getElementById('step1to2Progress');
        const step2to3Progress = document.getElementById('step2to3Progress');
        
        // Store the selected file
        let selectedFile = null;
        let isProcessing = false; // Flag to prevent multiple requests
        let currentStep = 1;

        // Function to update step indicators
        function updateStepIndicators(step) {
            // Reset all indicators
            [step1Indicator, step2Indicator, step3Indicator].forEach((indicator, index) => {
                if (index + 1 <= step) {
                    indicator.className = 'w-10 h-10 bg-gradient-to-r from-blue-500 to-purple-600 text-white rounded-full flex items-center justify-center font-bold';
                } else {
                    indicator.className = 'w-10 h-10 bg-gray-300 text-gray-600 rounded-full flex items-center justify-center font-bold';
                }
            });
            
            // Update progress bars
            if (step >= 2) {
                step1to2Progress.style.width = '100%';
            } else {
                step1to2Progress.style.width = '0%';
            }
            
            if (step >= 3) {
                step2to3Progress.style.width = '100%';
            } else {
                step2to3Progress.style.width = '0%';
            }
        }
        
        // Function to show specific step
        function showStep(step) {
            currentStep = step;
            updateStepIndicators(step);
            
            // Hide all steps
            [step1Content, step2Content, step3Content].forEach(content => {
                content.classList.add('hidden');
            });
            
            // Show current step
            switch(step) {
                case 1:
                    step1Content.classList.remove('hidden');
                    break;
                case 2:
                    step2Content.classList.remove('hidden');
                    break;
                case 3:
                    step3Content.classList.remove('hidden');
                    break;
            }
        }

        // Handle upload button click - trigger file input
        uploadButton.addEventListener('click', function(e) {
            e.preventDefault();
            console.log('Upload button clicked'); // Debug log
            imageUpload.click();
        });
        
        // Handle file selection with simplified processing
        imageUpload.addEventListener('change', function(e) {
            const file = e.target.files[0];
            if (!file) return;
            
            // Validate file type
            if (!file.type.startsWith('image/')) {
                alert('Please select a valid image file (JPG, PNG, GIF, etc.)');
                imageUpload.value = '';
                return;
            }
            
            // Validate file size (limit to 5MB to prevent freezing)
            const maxSize = 5 * 1024 * 1024; // 5MB in bytes
            if (file.size > maxSize) {
                alert('File size too large. Please select an image smaller than 5MB.');
                imageUpload.value = '';
                return;
            }
            
            // Show loading indicator
            const originalText = uploadButton.innerHTML;
            uploadButton.innerHTML = '<span class="flex items-center space-x-2"><svg class="w-5 h-5 animate-spin" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path></svg><span>Processing...</span></span>';
            uploadButton.disabled = true;
            
            selectedFile = file; // Store the file for later use
            
            // Simplified file reading with timeout
            const reader = new FileReader();
            let timeoutId;
            
            const resetButton = () => {
                uploadButton.innerHTML = originalText;
                uploadButton.disabled = false;
                if (timeoutId) clearTimeout(timeoutId);
            };
            
            // Set timeout to prevent hanging
            timeoutId = setTimeout(() => {
                alert('File processing timed out. Please try a smaller image.');
                resetButton();
                imageUpload.value = '';
            }, 5000); // 5 second timeout
            
            reader.onload = function(e) {
                try {
                    clearTimeout(timeoutId);
                    
                    // Simple preview without canvas optimization to prevent freezing
                    imagePreview.src = e.target.result;
                    
                    resetButton();
                    showStep(2);
                    
                } catch (error) {
                    console.error('Error processing image:', error);
                    alert('Error processing image. Please try a different file.');
                    resetButton();
                    imageUpload.value = '';
                }
            };
            
            reader.onerror = function() {
                clearTimeout(timeoutId);
                alert('Error reading file. Please try again.');
                resetButton();
                imageUpload.value = '';
            };
            
            // Use readAsDataURL for the file
            try {
                reader.readAsDataURL(file);
            } catch (error) {
                console.error('Error starting file read:', error);
                alert('Error reading file. Please try again.');
                resetButton();
                imageUpload.value = '';
            }
        });

        // Handle face detection button
        detectButton.addEventListener('click', async function() {
            // Prevent multiple simultaneous requests
            if (isProcessing) {
                return;
            }
            
            if (!selectedFile) {
                alert('Please select an image first');
                return;
            }

            isProcessing = true; // Set flag to prevent multiple requests
            
            // Show loading state
            detectButton.textContent = 'Detecting...';
            detectButton.disabled = true;

            try {
                // Create FormData with the image
                const formData = new FormData();
                formData.append('image', selectedFile, selectedFile.name);

                // Make API request
                const response = await fetch('http://localhost:5181/api/Search', {
                    method: 'POST',
                    body: formData
                });

                if (!response.ok) {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }

                const results = await response.json();
                
                // Clear previous results
                const resultsGrid = step3Content.querySelector('.grid');
                resultsGrid.innerHTML = '';
                
                // Generate and display results
                generateResultCards(results);
                
                // Update results count
                const resultsCount = step3Content.querySelector('p.text-gray-600');
                resultsCount.textContent = `We found ${results.length} potential matches in our database`;
                
                // Show step 3
                showStep(3);
                
                // Smooth scroll to results
                step3Content.scrollIntoView({ behavior: 'smooth', block: 'start' });
                
            } catch (error) {
                console.error('Error:', error);
                alert('Error detecting face: ' + error.message);
            } finally {
                // Reset button state and flag
                detectButton.textContent = 'Detect Face';
                detectButton.disabled = false;
                isProcessing = false; // Reset the flag
            }
        });
        
        // Add event handler for change image button
        changeImageButton.addEventListener('click', function() {
            showStep(1);
            imageUpload.value = ''; // Clear the file input
        });
        
        // Add event handler for restart button
        restartButton.addEventListener('click', function() {
            // Reset everything
            selectedFile = null;
            imageUpload.value = '';
            imagePreview.src = '';
            isProcessing = false;
            
            // Clear results
            const resultsGrid = step3Content.querySelector('.grid');
            resultsGrid.innerHTML = '';
            
            // Go back to step 1
            showStep(1);
        });
        
        // Function to generate result cards from API response
        function generateResultCards(results) {
            const resultsGrid = step3Content.querySelector('.grid');
            
            results.forEach((result, index) => {
                // Create result card
                const card = document.createElement('div');
                card.className = 'bg-white/70 backdrop-blur-sm rounded-3xl shadow-xl border border-gray-100 p-6 transform transition duration-500 ease-in-out opacity-0 translate-y-4 hover:scale-105';
                card.id = `result${index + 1}`;
                
                // Convert score to percentage
                const percentage = Math.round(result.score * 100);
                
                // Get color gradient based on score
                let colorGradient = 'from-gray-400 to-gray-500';
                if (percentage >= 80) {
                    colorGradient = 'from-green-400 to-emerald-500';
                } else if (percentage >= 60) {
                    colorGradient = 'from-yellow-400 to-orange-500';
                } else if (percentage >= 40) {
                    colorGradient = 'from-orange-400 to-red-500';
                }
                
                // Use the image filename directly or provide a data URL placeholder
                const imageUrl = result.image;
                
                card.innerHTML = `
                    <div class="text-center">
                        <div class="relative inline-block mb-4">
                            <img src="${imageUrl}" alt="Match" class="w-32 h-32 rounded-2xl object-cover mx-auto" onerror="this.src='data:image/svg+xml;base64,'">
                            <div class="absolute -bottom-2 -right-2 bg-gradient-to-r ${colorGradient} text-white text-xs font-bold px-3 py-1 rounded-full shadow-lg">
                                ${percentage}%
                            </div>
                        </div>
                        <h4 class="text-xl font-bold text-gray-800 mb-4">${result.title || 'Unknown'}</h4>
                        <div class="space-y-2 text-sm text-left bg-gray-50 rounded-xl p-4 mb-4">
                            <p class="flex items-center text-gray-600">
                                <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                                </svg>
                                File: ${result.image}
                            </p>
                            <p class="flex items-center text-gray-600">
                                <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
                                </svg>
                                Confidence: ${percentage}%
                            </p>
                        </div>
                        <button class="block w-full bg-gradient-to-r from-blue-500 to-purple-600 text-white text-sm font-medium py-3 px-4 rounded-xl hover:shadow-lg transition">
                            View Details
                        </button>
                    </div>
                `;
                
                resultsGrid.appendChild(card);
                
                // Animate the card with a delay
                setTimeout(() => {
                    card.classList.remove('opacity-0', 'translate-y-4');
                    card.classList.add('opacity-100', 'translate-y-0');
                }, 100 * (index + 1));
            });
        }
    </script>
</body>
</html>